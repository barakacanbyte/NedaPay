'use client';

// This is a simplified wagmi configuration that doesn't rely on imports from wagmi
// It's designed to be compatible with Vercel's build process

const config = {
  autoConnect: true,
  connectors: [],
  provider: null,
};

export default config;
