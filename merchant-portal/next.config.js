/** @type {import('next').NextConfig} */
const nextConfig = {
  // Using Netlify's Next.js plugin for server-side rendering
  // No need for static export configuration
}

module.exports = nextConfig
